bugfixes:
  - >-
    microsoft.ad.domain_child - Fix return document key so it displays when using the standard Ansible documentation
    tools.
  - >-
    microsoft.ad.service_account - Fix return document key so it displays when using the standard Ansible documentation
    tools.
